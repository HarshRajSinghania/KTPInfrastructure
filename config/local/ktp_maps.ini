; KTPMatchHandler / KTPAdminAudit map list config
; Profile: Local Development
;
; OWNER: afraznein/KTPDoDServerConfig, at
;   serverfiles/dod/addons/ktpamx/configs/ktp_maps.ini
; That is the file the fleet loads and the only one worth editing to change what
; a server does. This one is bound into the Docker dev stack by
; docker-compose.local.yml and is the default input of scripts/spatial_map_registry.py,
; so it is loaded -- but only ever by the dev stack.
;
; It is NOT a mirror, despite the line below, and is not kept in sync. Measured
; 2026-09-15 against live (md5 e70b3033784a458c8dad0f2d0e3e4f9f, uniform on all 24):
; this copy has 32 sections to live's 25. It lacks dod_anzio3_b1 and dod_pandemic_aim,
; and keeps 9 entries live dropped as unreachable. The extra entries are deliberate
; here -- config/local/dod-configs/ still ships their configs and the registry tests
; discover against them -- so do not "fix" the difference by resyncing.
;
; MIRROR OF PRODUCTION (NY1 2026-07-11): KTPMatchHandler parses the sectioned
; format ([map] + config/name/type) to resolve the match-time map config
; (exec_map_config -> configs/ktp_<map>.cfg -> ktpbasic.cfg -> mp_clan_match 1).
; The old simple "map = name" local format made every lookup MISS, so clan
; mode was never armed and mp_clan_restartround silently no-opped at go-live.
; The referenced configs/*.cfg files must exist in dod/configs/ (see
; docs or docker cp from prod).

; ==========================================
; KTP Match Handler - Map Configuration
; ==========================================
;
; New section-based format (v0.4.0+)
; Each map has its own [section] with config, name, and type
;
; Format:
; [map_name_without_bsp]
; config = config_file.cfg
; name = Display Name
; type = competitive
;
; ==========================================
; POPULAR MAPS (ordered by usage)
; ==========================================

[dod_anzio2_test3]
config = ktp_anzio.cfg
name = anzio2 test3
type = competitive

[dod_anzio_test4d]
config = ktp_anzio.cfg
name = anzio test4d
type = competitive

[dod_armory_b6]
config = ktp_armory.cfg
name = Armory
type = competitive

[dod_harrington]
config = ktp_harrington.cfg
name = Harrington
type = competitive

[dod_lennon5_b1]
config = ktp_lennon.cfg
name = Lennon5
type = competitive

[dod_railroad2_s9a]
config = ktp_railroad2.cfg
name = Railroad2
type = competitive

[dod_railyard_s9d]
config = ktp_railyard.cfg
name = Railyard
type = competitive

[dod_saints2_b3e]
config = ktp_saints.cfg
name = Saints2 b3e
type = competitive

; ==========================================
; REMAINING MAPS (alphabetical)
; ==========================================

[dod_aleutian]
config = ktp_aleutian.cfg
name = Aleutian
type = competitive

[dod_anjou_a3]
config = ktp_anjou.cfg
name = Anjou A3
type = competitive

[dod_anjou_a4]
config = ktp_anjou.cfg
name = Anjou A4
type = competitive

[dod_anjou_a5]
config = ktp_anjou.cfg
name = Anjou A5
type = competitive

[dod_anzio]
config = ktp_anzio.cfg
name = Anzio
type = competitive

[dod_avalanche]
config = ktp_avalanche.cfg
name = Avalanche
type = competitive

[dod_cal_sherman2]
config = ktp_cal_sherman2.cfg
name = Cal Sherman 2
type = competitive

[dod_donner]
config = ktp_donner.cfg
name = Donner
type = competitive

[dod_emmanuel]
config = ktp_emmanuel.cfg
name = Emmanuel
type = competitive

[dod_halle]
config = ktp_halle.cfg
name = Halle
type = competitive

[dod_kalt]
config = ktp_kalt.cfg
name = Kalt
type = competitive

[dod_lennon2]
config = ktp_lennon.cfg
name = Lennon2
type = competitive

[dod_lennon2_b1]
config = ktp_lennon2_b1.cfg
name = Lennon2 B1
type = competitive

[dod_lennon_b2]
config = ktp_lennon_b2.cfg
name = Lennon B2
type = competitive

[dod_lennon_b3]
config = ktp_lennon_b3.cfg
name = Lennon B3
type = competitive

[dod_lindbergh_b1]
config = ktp_lindbergh_b1.cfg
name = Lindbergh
type = competitive

[dod_merderet]
config = ktp_merderet.cfg
name = Merderet
type = competitive

[dod_muhle_b2]
config = ktp_muhle_b2.cfg
name = Muhle
type = competitive

[dod_northbound]
config = ktp_northbound.cfg
name = Northbound
type = competitive

[dod_railroad]
config = ktp_railroad1.cfg
name = Railroad
type = competitive

[dod_railroad2_test]
config = ktp_railroad2.cfg
name = Railroad2 Test
type = competitive

[dod_saints2_b2]
config = ktp_saints.cfg
name = saints2 b2
type = competitive

[dod_solitude2]
config = ktp_solitude2.cfg
name = Solitude2
type = competitive

[dod_thunder2]
config = ktp_thunder2.cfg
name = Thunder2
type = competitive